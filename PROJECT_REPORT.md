# 病理联邦学习通信压缩 Demo 项目报告

## 1. 项目定位

本项目面向多中心病理 AI 协作场景，模拟 3 家医院在数据不出院的条件下共同训练二分类病理 patch 模型，并比较不同梯度通信压缩策略对模型 AUC 和通信开销的影响。

项目目标不是替代真实临床系统，而是构建一个可复现的最小实验平台，用于验证：

1. 联邦学习可以避免原始病理数据跨医院流动。
2. 梯度通信是多中心训练中的重要开销。
3. Top-K 稀疏化、SignSGD 量化和 error feedback 可以显著降低通信负担。

## 2. 场景设定

真实多中心病理 AI 训练面临几个典型问题：

- 病理数据体量大，WSI 原图传输成本高。
- 医院数据涉及隐私和合规要求，不能随意集中。
- 不同医院染色风格和扫描流程不同，数据分布存在 Non-IID 差异。
- 联邦学习虽然保护原始数据，但每轮梯度或模型更新仍会产生通信开销。

本 Demo 使用 CPU 友好的合成 PCam-like patch 数据模拟病理图像，并用不同颜色扰动模拟 3 家医院的染色差异。后续可以替换为 `torchvision.datasets.PCAM` 或真实病理 patch。

## 3. 方法设计

### 3.1 FedAvg 基线

FedAvg 是联邦学习中最经典的基线方法：

1. 中心服务器下发当前全局模型。
2. 每家医院在本地数据上训练若干 epoch。
3. 医院上传本地模型更新。
4. 服务器按样本量加权平均，得到下一轮全局模型。

在本项目中，FedAvg 作为 100% 通信开销的基线。

### 3.2 Top-K 稀疏化

Top-K 的思想是只上传绝对值最大的 K% 模型更新，其余位置置零。

直觉是：梯度中幅值较大的分量通常对模型更新更重要，小幅值分量可以暂时丢弃。

本项目默认 `k_ratio=0.1`，即只上传 10% 梯度值载荷。

### 3.3 SignSGD 量化

SignSGD 将梯度值从 32-bit 浮点数压缩成方向信息：

```text
positive update -> +1
negative update -> -1
zero update     ->  0
```

如果只考虑梯度值载荷，Top-K + SignSGD 的通信比例约为：

```text
10% * 1 bit / 32 bits = 0.3125%
```

这就是本 Demo 中 `0.312%` 通信量的来源。

### 3.4 Error Feedback 残差补偿

单纯压缩会丢失大量更新信息，尤其是 SignSGD 只保留方向，容易导致精度下降。

Error feedback 的做法是：客户端保存本轮没有成功上传的残差，下一轮训练后把残差加回待压缩更新中。

简化公式：

```text
corrected_update_t = local_update_t + residual_t
compressed_t       = Compress(corrected_update_t)
residual_{t+1}     = corrected_update_t - compressed_t
```

这个机制让被压缩丢掉的信息不会永久消失，是压缩联邦学习中非常关键的稳定化技巧。

## 4. 实验配置

| 项目 | 设置 |
|---|---|
| 客户端数量 | 3 家模拟医院 |
| 数据 | 合成 PCam-like 病理 patch |
| 医院差异 | ColorJitter 模拟染色差异 |
| 模型 | Tiny CNN |
| 设备 | CPU 可运行 |
| 指标 | AUC、通信比例、通信节省 |
| 默认样本量 | 600 patches / 医院 |
| 默认轮数 | 3 轮 |

运行命令：

```bash
python train_federated.py --rounds 3 --samples-per-hospital 600 --mode research --cpu
```

## 5. 当前实验结果

当前 CPU 实验结果如下：

| 方法 | AUC | 梯度值载荷通信比例 | 通信节省 |
|---|---:|---:|---:|
| FedAvg | 0.9937 | 100.000% | 0.00% |
| Top-K | 0.9908 | 10.000% | 90.00% |
| Top-K + Error Feedback | 0.9894 | 10.000% | 90.00% |
| Top-K + SignSGD | 0.2416 | 0.312% | 99.69% |
| Top-K + SignSGD + Error Feedback | 0.9878 | 0.312% | 99.69% |

关键观察：

1. 单纯 `Top-K + SignSGD` 虽然通信量最低，但 AUC 明显下降，说明极端压缩会破坏训练。
2. 加入 error feedback 后，`Top-K + SignSGD + EF` 在 0.312% 梯度值载荷下仍能接近 FedAvg。
3. 这说明通信压缩不是简单“少传一点”，而是需要配合误差补偿机制。

## 6. 项目价值

这个项目的价值不在于网页本身，而在于它形成了一个可解释的实验闭环：

```text
临床问题 -> 联邦学习设定 -> 通信瓶颈 -> 压缩策略 -> 精度/通信对比 -> 局限与下一步
```

对于 OmniPT 这类多中心病理 AI 场景，该项目可以作为一个通信优化方向的入门验证：

- 原始数据不出医院。
- 只上传模型更新。
- 通过压缩减少跨中心通信压力。
- 用 error feedback 缓解压缩带来的精度损失。

## 7. 局限性

本 Demo 仍然是原型验证，存在明显局限：

1. 当前默认数据是合成 patch，不等价于真实 PCam 或真实 WSI。
2. 模型是 Tiny CNN，不是临床级病理基础模型。
3. 当前通信量统计主要计算梯度值载荷，尚未完整计算索引编码、协议开销和安全聚合开销。
4. 没有加入隐私攻击评估，例如梯度反演风险。
5. 没有模拟真实医院间样本量不均衡和疾病分布偏移。

这些局限不是缺点，而是后续工作的入口。

## 8. 下一步计划

优先级从高到低：

1. 替换为真实 PatchCamelyon 数据集。
2. 加入更严格的 Non-IID 划分，例如每家医院阳性比例不同。
3. 统计完整通信开销，包括索引、符号、mask、协议头。
4. 将 Tiny CNN 替换为 ResNet18，在 Colab T4 上运行。
5. 对比不同 `k_ratio`：1%、5%、10%、20%。
6. 加入多随机种子，报告均值和标准差。
7. 接入 Flower 多客户端进程，形成更真实的联邦系统演示。

## 9. 一句话总结

本项目证明了一个可行方向：在多中心病理 AI 场景中，联邦学习可以避免原始数据集中，而 Top-K + SignSGD + error feedback 可以在极低梯度通信载荷下维持接近 FedAvg 的模型效果。
